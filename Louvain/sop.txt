


1. The Louvain algorithm is sensitive to node ordering, leading to unstable and non-deterministic community detection results.
-The Louvain algorithm is an inherently stochastic method, meaning it can produce vastly different community partitions each time it is run on the exact same network (Pankratz et al., 2024). This non-deterministic behavior occurs because community detection algorithms often rely on randomized heuristic procedures to navigate massive solution spaces (Morea & De Stefano, 2024). Specifically, the Louvain algorithm is highly sensitive to initial conditions, such as the starting partition of the network, and its final output is strictly dependent on the random sequence in which individual nodes are examined and processed (Wang et al., 2024). Because the algorithm identifies a local modularity maximum based on this randomized node ordering, multiple executions will yield highly variable and unstable community structures (Morea & De Stefano, 2024; Wang et al., 2024). Ultimately, this reliance on random initialization and local decision-making not only compromises the consistency of the results but can also lead to the creation of weakly connected or internally disconnected communities (Pankratz et al., 2024).

Figure 1.1 [Impact of Node Ordering on Community Detection Outcomes] [A series of three identical network graphs, each displaying a visually distinct final community partition (indicated by different node colors) resulting from the Louvain algorithm. Below the graphs, a histogram displays the wide distribution in the total number of detected communities across 100 independent runs on the same network.]
Figure 1.1 shows that the Louvain algorithm yields highly unstable and non-deterministic results due to its reliance on randomized initial conditions
. Because the algorithm evaluates local modularity gains sequentially, the final partition depends heavily on the exact order in which nodes are introduced and processed
. The simulation demonstrates how different randomized input orderings on the exact same network topology cause the algorithm to get trapped in different local optima, producing entirely different community structures and a highly variable number of total communities across multiple executions
.

Actual : Figure 1.1 shows that the Louvain algorithm produces non-deterministic and unstable community detection results when applied to the real-world C-Town water distribution network. Across 30 independent runs on the exact same network topology, the algorithm yielded a varying number of detected communities ranging from 17 to 22, with no single result consistently dominating — demonstrating that no single "correct" partition is reliably reproduced. The right panel reinforces this instability by plotting the modularity score Q per run, which fluctuates erratically between 0.888 and 0.899 across the 30 trials. Critically, these variations in community count and modularity quality are not caused by any change in the network itself — only the internal node ordering (random seed) differs between runs. Because the algorithm evaluates local modularity gains sequentially based on this randomized order, it becomes trapped in different local optima each time, producing structurally divergent partitions that share only comparable — but never identical — modularity scores. This directly confirms the non-deterministic behavior described in SOP 1: the Louvain algorithm's strict dependence on node ordering makes its output inherently unstable, compromising the reproducibility and reliability of community detection results on a real-world water network.

2. The Louvain algorithm relies on modularity optimization, which introduces a resolution limit that prevents the detection of smaller communities within large-scale networks.
-Although the Louvain method is a highly efficient algorithm for community detection, its fundamental reliance on modularity maximization makes it susceptible to the well-known resolution limit problem (Sahu, 2023; Yao et al., 2023). Because of this inherent flaw, optimizing for Q-modularity fails to identify or resolve communities that fall below a specific scale or size threshold (Gharaghooshi et al., 2020; Sahu, 2023; Yao et al., 2023). Consequently, the algorithm often obscures fine-grained structures by incorrectly merging nodes from distinctly different small communities into larger groups in its final partition (Yao et al., 2023). Furthermore, this resolution limit stands in direct contrast to the field of view limit, a separate structural issue that causes the overpartitioning of communities with large diameters (Gharaghooshi et al., 2020).
Figure 1.2 [Resolution Limit in Modularity Optimization] [A side-by-side comparison of a network consisting of a ring of small, interconnected cliques. The left panel shows the ground-truth community structure where each small clique is correctly identified as an individual community. The right panel shows the Louvain algorithm's output, where adjacent small cliques are incorrectly merged together into larger communities.]
Figure 1.2 shows that optimizing for standard modularity intrinsically fails to detect communities below a specific scale threshold
. Using a "ring of cliques" benchmark network, the simulation illustrates how the Louvain algorithm is biased toward grouping smaller, distinct sub-communities into larger partitions
. Because modularity evaluates internal links relative to the total number of links in the entire global network, the algorithm incorrectly merges structurally meaningful cliques simply because they fall below the resolution limit
.
Actual: Figure 1.2 shows that the Louvain algorithm fails to correctly identify small, structurally well-defined communities when they fall below the modularity resolution threshold. The benchmark network is a ring of 12 fully-connected 3-node cliques, where the ground truth — shown in the left panel — assigns each clique its own distinct community colour, yielding exactly 12 communities. The right panel shows the Louvain output on the identical network: several adjacent cliques are incorrectly merged into a single, larger community. This is most visibly evident in the cluster of nodes 12–17 on the right side of the ring, where two neighbouring cliques that are distinctly separate in the ground truth are collapsed into a single red community by Louvain. The mechanism behind this is the resolution limit inherent to modularity maximization — because modularity evaluates the density of internal edges relative to the total edge count of the entire network, small cliques interconnected only by single bridge edges collectively appear statistically insignificant, causing the algorithm to treat merging them as a modularity gain. This directly supports SOP 2: the Louvain algorithm's reliance on modularity optimization introduces a scale bias that systematically prevents the detection of smaller communities, obscuring fine-grained structure that is clearly present in the network topology.

3. The Louvain algorithm employs a greedy, local optimization strategy that fails to guarantee globally optimal community partitions.
-The Louvain algorithm operates on a greedy optimization process that iteratively updates community assignments node by node to maximize immediate, local modularity gains (Wang & Kolter, 2020). Because these reassignments are evaluated solely on localized gains without enforcing internal connectivity constraints, the algorithm violates fundamental structural assumptions and can produce internally non-cohesive or completely disconnected communities (Santos et al., 2025; Wang & Kolter, 2020). This reliance on local decision-making makes the algorithm highly susceptible to getting trapped in local optima, a limitation that is particularly evident when analyzing complex networks with nuanced structures (Hernández et al., 2025; Wang & Kolter, 2020).
Furthermore, because the algorithm applies these greedy updates sequentially during its local moving phase, the final partition is highly sensitive to the exact order in which nodes are processed. Consequently, different node orderings can yield entirely different partitions that share comparable modularity scores, introducing a level of non-determinism that compromises the reproducibility of the results (Santos et al., 2025).
Figure 1.3 [Formation of Disconnected Communities due to Greedy Optimization] [A two-panel diagram of a network illustrating the local moving phase. Panel A shows a connected community containing a central node that acts as a bridge between two sub-groups. Panel B shows the network after this bridge node is reassigned to an adjacent community, leaving the original community split into two entirely disconnected groups of nodes.]
Figure 1.3 shows that the Louvain algorithm's strict reliance on maximizing immediate, local modularity gains can produce arbitrarily poorly connected or entirely disconnected communities
. In the first stage, a specific node acts as a bridge connecting different components of its assigned community
. In the next stage, the algorithm greedily reassigns this bridge node to a neighboring community because the move offers a higher immediate modularity score
. Because the algorithm does not evaluate the long-term global impact of this move or enforce internal connectivity constraints, the remaining nodes stay in their current assignment, leaving their community structurally fragmented and internally disconnected
.
Actual: Figure 1.3 shows that the Louvain algorithm's greedy local optimization strategy can produce internally disconnected communities by relocating structurally critical bridge nodes based solely on immediate, localized modularity gain — without any regard for the global structural consequences of that move.
The synthetic network is built with two bridge nodes — nodes 6 and 13 (shown in red, connected by dashed red edges) — that serve as the only connectors holding four separate blue sub-groups of Community A together into one cohesive partition. Panel A illustrates the initial intact state: all four sub-groups ({0,1,2}, {7,8,9}, {3,4,5}, {10,11,12}) are reachable through the bridge nodes, forming a single connected community.
Panel B shows what happens after the greedy local-moving phase: both bridge nodes 6 and 13 are pulled into the dense orange neighbour community (nodes 14–17, a fully-connected K4) because that reassignment offers a higher immediate modularity increment. With the bridges removed, Community A instantly collapses into 4 completely isolated, disconnected islands. The algorithm never evaluates this long-term structural damage — it enforces no internal connectivity constraint and has no global foresight.
This directly and visually confirms SOP 3: the Louvain algorithm's strict node-by-node greedy optimization can break apart what was a single valid community into multiple disconnected fragments, fundamentally violating the assumption that a detected community represents a coherent, internally connected subgraph.